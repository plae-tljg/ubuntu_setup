# Common Directory of Ubuntu

Place to store `.desktop` files, `/usr/share/applications` or inside `~/.local/`.  

Place to store service files, `/etc/systemd/system/` or inside `/lib/systemd/system/`.  
